# Brickgame_v2.0
Проект реализует две классические игры - Tetris и Snake - на языках C/C++ с использованием двух типов интерфейсов: консольного (ncurses) и десктопного (Qt). Проект включает систему сохранения рекордов для каждой игры в отдельные текстовые файлы.

# 🎮 Возможности
- Две классические игры в одном приложении:
    - Tetris с семью типами фигур и классической механикой
    - Snake с постепенным усложнением и системой уровней
- Два типа интерфейса на выбор:
    - Консольный интерфейс с использованием библиотеки ncurses
    - Графический интерфейс на фреймворке Qt
- Система подсчета очков и уровней сложности  
- Система сохранения и отображения рекордов для каждой игры
- Полноценная система тестирования (unit-тесты)  
- Подробная документация (Doxygen)  
- Поддержка различных платформ (Linux, macOS)
- Модульная архитектура с разделением игровой логики и интерфейса

# 🛠️ Требования
- Компиляторы: 
    - GCC (поддерживающий стандарт C11)  
    - G++ (поддерживающий стандарт C++20)
- Библиотеки:
    - ncurses (для консольного интерфейса)
    - Qt6 Widgets (для десктопного интерфейса)
- Системы тестирования:
    - Check Framework (для тестиров на C)
    - Google Test (для тестов на C++)  
- Doxygen (для генерации документации)  
- CMake (версия 3.5 или выше) для сборки  

# 📦 Установка и сборка
```bash
# Стандартная сборка (с текстовым файлом для рекордов)   
make install   
# Запуск игры (при запуске предложит выбрать игру)
make play или ./build/BrickGameV2.0
```
# 🎯 Управление

- Tetris

← → или A D - движение влево/вправо

↓ или S - ускоренное падение

Пробел - поворот фигуры

- Snake

← / ↑ / → / ↓ или WASD - управление направлением змейки

Пробел - временное ускорение змейки

- Общее

P - пауза/продолжение игры

Enter - начать новую игру

Q или Esc - выход из игры

# 🧪 Тестирование
Проект включает комплекс unit-тестов:

```bash
# Запуск тестов (текстовое сохранение)
make test

# Проверка утечек памяти
make check_valgrind
```
### Генерация отчетов о покрытии кода
```bash
make gcov_report
```
# 📚 Документация
Для генерации документации с помощью Doxygen:

```bash
# Документация будет создана в папке doxygen/html/.
make dvi
```

# 📁 Структура проекта

```
brick_game/              # Исходные файлы игровой логики
├── tetris/              # Основной модуль тетриса
├── snake/               # Основной модуль змейки
gui/                     # Графический интерфейс
├── cli/                 # Реализация на ncurses
├── desktop/             # Реализация на Qt
tests/                   # Тесты
brickgame.cc             # Главный файл запускаюший все
brickgame.h              # Хедер для главного файла
lib_struct.h             # Хедер со спецификацией библиотеки
inline.h                 # Встроенные константы
CMakeLists.txt           # Файл сборки с помощью CMake
Makefile                 # Файл сборки
Doxyfile                 # Конфигурация Doxygen
```
# Дополнительные цели Makefile
```bash
make dist # создание дистрибутива

make clean # очистка собранных файлов

make uninstall # полная очистка проекта

make lcov_report # отчет LCOV о покрытии кода

make gcov_report # отчет GCOVR о покрытии кода
```

# 📊 Система очков

Tetris

- 1 линия: 100 очков

- 2 линии: 300 очков

- 3 линии: 700 очков

- 4 линии: 1500 очков

- Уровень сложности увеличивается каждые 600 очков (максимум 10 уровень).

Snake

- Каждое яблоко: +1 к счету

- Переход на новый уровень: каждые 5 очков (максимум 10 уровень)

- Скорость игры увеличивается с каждым уровнем

- Победа: заполнение всего поля змейкой

# 🔧 Архитектура проекта

Проект использует модульную архитектуру с четким разделением ответственности:

1.Игровая логика (brick_game/) - независимые модули для каждой игры

2.Пользовательский интерфейс (gui/) - отдельные реализации для разных платформ

3.Движок игры (brickgame.*) - связывает логику и интерфейс

4.Общие компоненты (lib_struct.h, inline.h) - shared кодовбаза

Такая архитектура позволяет:

- Легко добавлять новые игры

- Реализовывать новые типы интерфейсов

- Тестировать компоненты независимо

- Поддерживать кодовую базу

# 📝 Примечания

- Рекорды сохраняются в файлах tetris_score.txt и snake_score.txt

- Для работы десктопной версии требуется установленный Qt6

- Консольная версия требует библиотеку ncurses

- Проект разработан в образовательных целях